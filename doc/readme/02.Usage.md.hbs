Usage
---------

### Add Parent Selector

`cssspace.addParent(css, parent)` method parses css string and add parent selector to each selectors.

```javascript
{{{read 'example/example-add-parent.js'}}}
```
